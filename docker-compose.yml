services:

  sharebox:

    build:
      context: .
      dockerfile: Dockerfile

    container_name: sharebox

    ports:
      - "4040:4040"

    volumes:
      - sharebox-data:/app/uploads

    restart: unless-stopped

volumes:

  sharebox-data: